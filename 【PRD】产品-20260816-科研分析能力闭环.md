# 【PRD】产品-20260816-科研分析能力闭环

---

## 1. 修订记录

| 版本号 | 修订日期 | 修订内容 | 修订者 |
|--------|----------|----------|--------|
| V1.0 | 2026-08-16 | 初稿 | 产品 |

---

## 2. 相关链接

| 事项 | 内容 |
|------|------|
| 产品需求池 | 待补充 |
| 设计稿 | 待补充 |

---

## 3. 需求概述

### 背景

运动能力分析系统已经具备体质数据展示、课题档案、AI 写作和学术搜索三个科研工作入口，但研究人员仍需要在数据分析、材料撰写和文献整理之间来回切换。当前版本在同一科研分析页面下建立了三个子模块，并通过课题档案、结构化写作工具和本地示例文献库串联起“发现问题—形成课题—准备材料—沉淀引用”的基本工作流。

### 核心需求

提供 `/research` 科研分析、`/research/writing` AI 写作、`/research/scholar` 学术搜索三个可直接使用的科研工作区。科研分析负责指标趋势、年级对比、短板分布、课题进度和数据导出；AI 写作提供 19 种写作工具及统一生成表单；学术搜索提供主题检索、来源/年份筛选、知识库收藏、引用保存和一键带入写作。

### 预期效果

让体育教研人员能够在一个系统内完成研究线索查看、课题立项记录、科研材料初稿生成和文献引用整理，减少重复录入和页面跳转。当前版本定位为可交互前端原型，生成内容和文献均为本地示例，正式上线前需接入权限、持久化、AI 服务和学术数据源。

---

## 4. 需求背景与目标

### 4.1 背景

当前科研工作区的主要问题和边界如下：

1. **数据与研究记录分散**：体质健康趋势、年级差异和核心短板需要在分析页面集中查看，课题进度需要可追踪记录。
2. **科研材料起草成本高**：开题、申报、中期、结题以及机关单位类材料结构相近，但过去缺少统一的表单和可编辑初稿工作区。
3. **文献整理缺少闭环**：研究人员需要快速搜索体育教育文献，保存知识点和标准引用，再回到写作页面使用。
4. **当前实现仍是前端原型**：示例数据、文献和草稿保存在浏览器本地，尚未提供跨设备同步、真实 AI 生成和在线学术检索。

### 4.2 目标

1. **工作流目标**：用户可从科研分析进入 AI 写作或学术搜索，并在学术搜索中将选中文献引用带入结题报告，核心路径在 3 次页面操作内完成。
2. **工具覆盖目标**：AI 写作中心按 5 个分类提供 19 个工具，覆盖学术教育、学生常用、职场精选和机关单位等截图范围内的写作类型。
3. **交互目标**：所有写作工具均提供主题、写作大纲、参考文献、写作风格、辅助信息、语言和篇幅控制，并支持生成后编辑、复制和导出。
4. **可用性目标**：科研三个子页面在桌面端和 390px 移动端无横向溢出；核心路由返回 HTTP 200；回归 QA 无控制台错误。

---

## 5. 需求范围与分工

### 5.1 需求范围

| 系统/模块 | 功能描述 | 优先级 |
|-----------|----------|--------|
| 科研分析 `/research` | 趋势图、年级对比、短板分布、课题列表、新建课题、科研数据导出 | P0 |
| AI 写作中心 `/research/writing` | 分类导航、工具搜索、最近使用和 19 个工具卡片 | P0 |
| AI 写作详情 `/research/writing/:slug` | 统一写作表单、参考资料上传、生成初稿、在线编辑、复制和导出 | P0 |
| 学术搜索 `/research/scholar` | 关键词检索、来源/年份筛选、文献详情、知识库和引用操作 | P0 |
| 科研数据与草稿本地存储 | 使用浏览器 `localStorage` 保存课题、工具最近使用、知识库、引用 | P1（原型能力） |
| AI 生成服务与学术数据库 | 接入真实模型、文献检索 API、鉴权和服务端持久化 | P1（后续） |

### 5.2 需求分工

| 角色 | 负责人 | 职责 |
|------|--------|------|
| 产品经理 | 产品 | 需求定义、内容边界、验收标准制定 |
| UI设计师 | 待分配 | 科研工作区、工具卡片和写作表单视觉规范 |
| 前端开发 | 待分配 | 路由、图表、表单、编辑器和响应式交互 |
| 后端开发 | 待分配 | 课题/文献/草稿服务、AI 调用和权限控制 |
| 测试工程师 | 待分配 | 路由、表单、异常、移动端和端到端测试 |
| 体育教研顾问 | 待确认 | 指标口径、课题模板和科研内容审核 |

---

## 6. 业务逻辑

### 6.1 业务流程图

```text
科研分析 /research
      │
      ├── 查看趋势、年级对比、短板分布
      ├── 新建课题档案 ──> 课题列表与进度
      ├── 导出科研数据 ──> CSV（Excel 兼容）
      │
      ├── AI 写作 /research/writing
      │       ├── 选择分类与工具
      │       ├── 填写主题、纲要、文献、风格、辅助信息
      │       └── 写作 ──> 在线编辑 ──> 复制 / 导出 Markdown
      │
      └── 学术搜索 /research/scholar
              ├── 输入关键词、来源、年份
              ├── 查看摘要与关键知识点
              ├── 加入知识库 / 保存引用
              └── 带入 AI 写作 ──> 结题报告表单中的引用文本
```

### 6.2 流程说明

1. 用户通过科研分析页顶部子导航进入三个科研子模块。
2. 科研分析页展示本地示例趋势和课题数据；用户可以新建课题，新增记录插入列表首位并保存到浏览器本地。
3. 用户进入 AI 写作中心，按“推荐写作、学术教育、学生常用、职场精选、机关单位”筛选工具，也可搜索工具名称和描述。
4. 用户在具体工具页填写主题，选择 AI 智能或指定大纲、AI 智能或手动上传参考文献，并选择专业、深奥或理论风格后点击“写作”。
5. 系统根据工具配置生成结构化 Markdown 初稿。初稿仅为前端示例生成内容，用户可以继续编辑、复制或下载。
6. 用户在学术搜索页检索本地示例文献，收藏知识库或保存标准引用；点击“带入 AI 写作”后，引用文本写入本地并打开课题结题报告工具。

---

## 7. 功能详细说明

### 7.1 科研分析

#### 功能定位

为体育教研人员提供学校体质健康趋势观察、年级效果比较、核心短板识别、课题进度管理和科研数据导出的统一入口。

#### 产品规则

1. 页面固定展示体质达标率、优良率、覆盖样本、跟踪周期和进行中课题 5 个指标卡。
2. 趋势区展示周期达标率和优良率折线；年级区展示四个年级的达标率和优良率柱状对比。
3. 核心短板以速度耐力、下肢爆发、柔韧性和协调性四类指标展示，分数越低越应优先干预。
4. 新建课题必须填写课题名称、负责人、计划样本数和计划结题日期；创建后默认阶段为“方案设计”、进度为 8%。
5. 导出操作生成带 UTF-8 BOM 的 CSV 文件，字段可被 Excel 直接打开。
6. 当前“周期对比/班级对比/年级对比”控件为前端原型选择状态，后续需要接入真实维度数据查询。

#### 字段定义

| 字段名 | 字段类型 | 是否必填 | 说明 |
|--------|----------|----------|------|
| project_id | string | 是 | 课题编号，如 `RS-2026-01` |
| title | string | 是 | 课题名称 |
| owner | string | 是 | 课题负责人或团队 |
| stage | string | 是 | 研究阶段，如数据采集、方案实施、成果整理 |
| progress | number | 是 | 课题进度百分比，0-100 |
| samples | number | 是 | 样本人数 |
| deadline | date | 是 | 计划结题日期 |
| period | string | 是 | 趋势周期，如 2026 春末 |
| pass | number | 是 | 体质达标率，百分比 |
| excellent | number | 是 | 体质优良率，百分比 |
| sprint | number | 是 | 30m 平均成绩，秒 |
| jump | number | 是 | 纵跳平均高度，厘米 |

#### 业务定义

- 课题数据初始来源为 `researchProjects`，用户新建记录保存到 `sport-research-projects`。
- 趋势、年级对比和短板图表当前使用 `extendedData.js` 中的本地示例数据。
- “进行中课题”统计 `progress < 100` 的记录。
- 导出文件名称为“体质健康科研数据.csv”，用于原型阶段的数据交换和验收。

#### 交互说明

- 点击“新建课题”打开模态框，提交后立即刷新课题列表并显示成功提示。
- 点击“导出科研数据”下载 CSV，不改变页面数据。
- 图表提供 Tooltip 和图例，桌面端双列展示，窄屏自动改为单列。

#### 异常处理

- 课题名称为空：应阻止提交并提示补充名称；当前原型使用默认值，后续需增加显式校验。
- 计划样本数为负数或非数字：应阻止提交并提示合法范围；后续接入接口时由前后端共同校验。
- 本地存储不可用：应回退到当前页面内存状态，并提示用户数据不会跨刷新保存。
- CSV 下载被浏览器拦截：保留页面数据，并提示用户允许下载或复制数据。

### 7.2 AI 写作中心与工具详情

#### 功能定位

提供截图范围内的写作工具目录和统一创作工作区，将不同文种的差异收敛为工具配置，降低科研材料和正式报告的起草成本。

#### 产品规则

1. 工具中心提供 5 个分类：推荐写作、学术教育、学生常用、职场精选、机关单位。
2. 当前共配置 19 个工具：文献综述；开题报告、课题申报材料、课题中期报告、课题结题报告；实习总结报告、心得体会、演讲稿；工作总结、调研报告、活动总结报告；情况汇报、社情民意报告、提案议案、职称评选报告、通知、思想感悟、先进事迹报告、新闻稿。
3. 每个工具必须有名称、分类、描述、输入标签、占位提示、推荐章节和功能点。
4. 主题为必填项，最多 100 个字符；未填写时不允许生成并显示错误提示。
5. 写作大纲支持“AI 智能”和“指定大纲”；指定大纲按换行拆分章节，未填写时回退到工具默认章节。
6. 参考文献支持“AI 智能”和“手动上传”；手动上传支持粘贴引用文本及 PDF、DOCX、TXT 文件名记录。
7. 写作风格固定提供“专业风格、深奥风格、理论风格”三个选项。
8. 生成结果必须带有工具名称、主题、语言、篇幅和风格信息，并以可编辑文本区呈现。
9. 最近使用最多保留 8 个工具，保存于浏览器本地；结果可复制到剪贴板或导出为 Markdown。
10. 当前生成逻辑为前端结构化示例，不代表真实 AI 事实、引用或研究结论，提交前必须人工核验。

#### 字段定义

| 字段名 | 字段类型 | 是否必填 | 说明 |
|--------|----------|----------|------|
| slug | string | 是 | 工具路由标识，如 `jietibaogao` |
| name | string | 是 | 工具名称 |
| category | string | 是 | 工具分类 |
| inputLabel | string | 是 | 主题字段标签，如标题、科研课题 |
| topic | string | 是 | 用户输入的标题、课题或主题，最长 100 字符 |
| outlineMode | enum | 是 | `AI 智能` / `指定大纲` |
| outline | string | 否 | 自定义章节，每行一个标题 |
| references | enum | 是 | `AI 智能` / `手动上传` |
| referenceText | string | 否 | 粘贴的标准引用或参考文献列表 |
| referenceFiles | string[] | 否 | 已选择的 PDF、DOCX、TXT 文件名 |
| style | enum | 是 | 专业风格 / 深奥风格 / 理论风格 |
| auxiliary | string | 否 | 关键词、背景、方法、成果和其他要求，最长 5000 字符 |
| language | enum | 是 | 中文 / 英文 |
| size | enum | 是 | 短（约 4000 字）/ 中（约 8000 字） |
| draft | string | 否 | 生成后的 Markdown 文本 |

#### 业务定义

- `/research/writing` 展示工具目录；`/research/writing/:slug` 展示具体工具详情。
- 生成器按 `profile.sections` 逐节生成内容，使用用户主题、风格和辅助信息填充占位文本。
- 学术搜索带入的引用写入 `sport-research-citation-draft`，进入结题报告时自动选择手动上传并预填引用文本。
- 生成成功后工具 slug 写入 `sport-writing-recent`，工具中心按最近使用顺序展示。

#### 交互说明

- 点击分类按钮切换工具集合，搜索框按名称和描述过滤当前分类。
- 工具卡片点击后进入详情页，详情页顶部提供“返回工具中心”和“导出初稿”。
- 指定大纲、手动上传和三种写作风格均使用分段按钮，切换后即时展示对应控件。
- 生成后结果区支持直接编辑，复制和导出按钮在无草稿时禁用。
- 移动端将右侧工具说明改为顶部信息区，表单和结果区改为单列。

#### 异常处理

- 主题为空：提示“请先填写{输入标签}”，不发起生成。
- 剪贴板不可用或无权限：提示用户直接选中文本复制，不影响草稿内容。
- 文件格式不在 PDF、DOCX、TXT 范围：前端不接受选择；服务端接入时仍需二次校验。
- 生成过程刷新或离开页面：当前草稿不持久化；后续接入服务端后应支持草稿恢复。
- AI 服务超时、限流或返回不合规内容：后续接入真实模型时需显示失败状态、允许重试，并保留人工审核提示。

### 7.3 学术搜索

#### 功能定位

为体育教育研究提供主题检索、来源与年份筛选、文献摘要阅读、关键知识点查看、知识库收藏、标准引用保存和写作引用带入能力。

#### 产品规则

1. 页面默认关键词为“体质健康”，数据来自 6 篇本地示例文献。
2. 检索范围包含标题、作者、来源、摘要和关键词；关键词匹配不区分大小写。
3. 来源支持全部来源、期刊论文、会议论文、学位论文；年份支持全部年份、近三年、近五年。
4. 结果列表展示文献类型、年份、来源、标题、作者、摘要和关键词。
5. 用户可以将文献加入或移出知识库，也可以保存标准引用并复制到剪贴板。
6. “带入 AI 写作”默认打开课题结题报告工具，并将当前文献的标准引用预填到引用文本。
7. 当前文献库为本地示例，不能视为真实数据库检索结果或已核验学术来源。

#### 字段定义

| 字段名 | 字段类型 | 是否必填 | 说明 |
|--------|----------|----------|------|
| paper_id | string | 是 | 文献编号，如 `P-2026-01` |
| title | string | 是 | 文献标题 |
| authors | string | 是 | 作者列表 |
| source | string | 是 | 期刊、会议或学位授予单位 |
| year | number | 是 | 出版年份 |
| type | enum | 是 | 期刊论文 / 会议论文 / 学位论文 |
| keywords | string[] | 否 | 文献关键词 |
| abstract | string | 是 | 文献摘要 |
| insight | string | 是 | 可复用关键知识点 |
| citation | string | 是 | 标准引用文本 |
| query | string | 否 | 用户输入的检索词 |
| sourceFilter | enum | 是 | 来源筛选值 |
| yearFilter | enum | 是 | 年份筛选值 |
| knowledgeIds | string[] | 否 | 已加入知识库的文献编号 |
| citationIds | string[] | 否 | 已保存引用的文献编号 |

#### 业务定义

- 检索结果由 `researchPapers` 过滤生成，空结果时展示空状态和调整关键词建议。
- 知识库编号保存于 `sport-research-knowledge`，引用编号保存于 `sport-research-citations`。
- 文献详情区展示关键知识点、摘要、标准引用和三个操作按钮。
- 带入写作只传递引用文本，不自动把摘要或结论写入最终稿。

#### 交互说明

- 点击“搜索文献”或在输入框按 Enter 触发检索，并将第一条结果设为当前文献。
- 点击结果卡片切换右侧详情；点击列表内操作按钮不会触发卡片切换。
- “加入知识库”按钮根据当前状态显示加入或移出；顶部实时显示收藏和引用数量。
- “复制引用”只复制标准引用；“带入 AI 写作”跳转到 `/research/writing/jietibaogao`。

#### 异常处理

- 无匹配结果：展示“没有匹配文献”，不清空当前本地收藏和引用记录。
- 剪贴板不可用：保留保存状态，提示用户手动复制。
- 本地存储不可用：保留当前页面会话状态，并提示无法跨刷新保存。
- 文献元数据缺失：正式接入数据源时应过滤无标题/作者/引用的记录，并标记待核验字段。

---

## 8. 数据埋点

当前项目为 H5/桌面端响应式前端，已完成页面交互但尚未接入正式埋点 SDK。以下事件为生产接入时的建议方案，字段中的 `user_id`、`session_id` 需遵循实际隐私和权限规范。

| 埋点功能 | 埋点页面 | 埋点事件 | 终端 |
|----------|----------|----------|------|
| 科研分析页面浏览 | `/research` | `page_view_research_analysis` | H5 |
| 新建课题提交 | `/research` | `form_submit_research_project` | H5 |
| 科研数据导出 | `/research` | `btn_click_research_export` | H5 |
| AI 写作中心浏览 | `/research/writing` | `page_view_writing_hub` | H5 |
| 写作工具卡片曝光 | `/research/writing` | `list_expose_writing_tools` | H5 |
| 写作工具打开 | `/research/writing/:slug` | `btn_click_writing_tool` | H5 |
| AI 写作生成 | `/research/writing/:slug` | `form_submit_writing_generate` | H5 |
| 草稿复制 | `/research/writing/:slug` | `btn_click_writing_copy` | H5 |
| 草稿导出 | `/research/writing/:slug` | `btn_click_writing_export` | H5 |
| 学术搜索页面浏览 | `/research/scholar` | `page_view_scholar` | H5 |
| 文献检索 | `/research/scholar` | `form_submit_scholar_search` | H5 |
| 检索结果曝光 | `/research/scholar` | `list_expose_scholar_results` | H5 |
| 加入知识库 | `/research/scholar` | `btn_click_scholar_knowledge` | H5 |
| 保存引用 | `/research/scholar` | `btn_click_scholar_citation` | H5 |
| 带入 AI 写作 | `/research/scholar` | `btn_click_scholar_to_writing` | H5 |

### 埋点事件说明

- `page_view_*`：页面完成加载后触发一次。
- `btn_click_*`：用户完成对应按钮点击后触发，建议附带工具 slug、文献编号或筛选条件。
- `form_submit_*`：表单通过前端校验并提交后触发，失败提交另记 `*_error` 事件。
- `list_expose_*`：列表进入可视区域后触发，建议仅对首屏和分页结果去重。

---

## 9. 其他补充说明

### 9.1 附录

- 运行地址：<http://162.211.183.169:8524/research>
- AI 写作中心：<http://162.211.183.169:8524/research/writing>
- 学术搜索：<http://162.211.183.169:8524/research/scholar>
- 页面路由入口：`src/App.jsx` 的 `RouteContent` 和 `src/ExtendedPages.jsx` 的 `ResearchCenter`。
- 数据配置：`src/extendedData.js` 中的 `researchProjects`、`researchPapers`、`writingToolProfiles`。
- 原型持久化键：`sport-research-projects`、`sport-writing-recent`、`sport-research-knowledge`、`sport-research-citations`、`sport-research-citation-draft`。

### 9.2 进度更新

- 2026-08-16：完成科研分析、AI 写作、学术搜索三模块产品报告初稿。
- 2026-08-16：安装本地 `product-requirement-document` skill 到 `/home/claude/.codex/skills/product-requirement-document`。
- 2026-08-16：确认 8524 服务正常，三类科研路由返回 HTTP 200；构建和全站 QA 已通过。

### 9.3 特殊说明

1. 本报告描述的是当前已实现的前端原型和可执行产品边界，不等同于已具备生产级 AI、学术数据库、账号权限和服务端数据能力。
2. 现有示例文献中的作者、期刊、年份、摘要和引用仅用于交互演示；正式科研使用前必须接入可核验来源并保留出处。
3. AI 生成结果必须经过研究人员人工核验，尤其是数据、引用、经费、结论和政策表述，不应直接作为论文或正式报告提交。
4. 后续生产化优先级建议为：服务端持久化与权限 > AI 服务接入与内容安全 > 学术数据库接入 > 埋点与运营分析 > 多人协同和版本管理。

---

**文档结束**
